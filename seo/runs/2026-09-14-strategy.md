# Brothers. SEO strategist run: 2026-09-14

- **Trigger:** manual, first run; the loop was installed in the same session.
- **Phase:** 1 (DR=0, org_keywords il=0, GSC=not connected).
- **Data:**
  - Ahrefs OK: REST API v3, country il, 7,407 units used (24,087 → 31,494).
  - GSC: none.
  - Panel 2026-09-14: 30 runs, 29 answered, 1 error.
    - ChatGPT `gpt-5`: 15 of 15 answered.
    - Gemini `gemini-3.1-pro-preview`: 14 of 15 answered.
    - Perplexity: not run, no API key.

**Candidates evaluated:** 28 seed keywords (21 returned data) and 150 matching terms, plus 8 SERPs
and 5 competitor top-page sets.

**Items:** 7 (todo 0, refresh 5, blocked 2). **Parked:** 7.

**Top 5:**

| # | Slug | Score | Keyword | Vol/KD |
|---|---|---|---|---|
| 1 | lp-weddings-big-center-retarget | 1.173 | צלם חתונות במרכז | 90/2 |
| 2 | lp-stills-retarget | 0.873 | צלם סטילס לחתונה | 120/– |
| 3 | price-guide-wedding-photographer-2026 | 0.871 | כמה עולה צלם לחתונה | 800/0 (blocked) |
| 4 | lp-weddings-small-retarget | 0.775 | צלם לחתונה קטנה | 70/– |
| 5 | city-rishon-lezion | 0.673 | צלם חתונות בראשון לציון | 40/– (blocked) |

**AI answers:**
- Brothers. was named in 0 of 29 answered runs and cited in 0.
- Top cited domains: mit4mit.co.il 14, midrag.co.il 10, luz-weddings.com 6,
  antonmislawsky.com 5, engaged.co.il 5, easy.co.il 4, t.co.il 4.

**Off-page opportunities surfaced:** 10.

**Directives:** none (there's no `growth/directives.md` yet).

## Gate

1. **PASS.** `backlog.json` parses; all 7 items have slug, primaryKeyword, status and score.
2. **PASS.** All 29 keyword volume/KD values match a file under `seo/data/ahrefs/2026-09-14/`
   (0 mismatches, 0 missing).
3. **PASS.** No item duplicates a live page's intent unless its status is `refresh`.
4. **PASS.** The top non-blocked item (lp-weddings-big-center-retarget) has actionable notes.
5. **PASS (install exception).** This is the install run, so the diff also covers
   `.claude/skills/`, `scripts/`, `package.json` (three npm scripts) and `growth/goals.json`.
   Nothing in `src/` or `public/` changed.

## Incidents

- **Gemini's first pass failed on all 15 prompts.** `gemini-2.5-pro` returned 404 ("no longer
  available to new users") even though the models list still includes it.
  - `scripts/aeo-panel.mjs` now falls through an ordered candidate list on a 404. It also gained
    `--engines` and `--merge`.
  - Gemini was re-run alone and merged. The failed pass isn't kept as data.
- **p06 (Gemini) returned 503 on the re-run.** It's recorded as an error, not as "not named".
- **The Ahrefs MCP server isn't connected locally.** The REST API with `AHREFS_MCP_KEY`
  returned 200 on every call.
- **`.claude/` is gitignored** in this repo. Only the skill file was force-added; the ignore rule
  is unchanged.

## Decisions and notes

- **Stills and small-weddings SERPs:** Ahrefs stored none for "צלם סטילס לחתונה" or
  "צלם לחתונה קטנה", so both items carry `serpUnchecked`.
- **No SEO investment** in city pages (except a possible Rishon LeZion page), the outdoor, concept
  and business-events LPs, or Ashdod. See the Decisions section in `strategy.md`.
- **Seven client questions** are in `strategy.md`; two of them block items.
- **The mit4mit profile and directory listings** are the first AI-answer lever, backed by the
  panel data.
- **Timing:** this first run happened before a client owner was named, on Netanel's instruction.
  It's research only; the output is a draft PR and nothing live changes.
